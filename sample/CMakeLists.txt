AUX_SOURCE_DIRECTORY(. SAMPLE_SOURCES)
INCLUDE_DIRECTORIES(${CAF_INCLUDE_DIRS})

FOREACH(SRC ${SAMPLE_SOURCES})
  STRING(REGEX REPLACE ".*/(.*)\\.cpp$" "\\1" TGT ${SRC})
  ADD_EXECUTABLE(${TGT} ${SRC})
  TARGET_LINK_LIBRARIES(${TGT} ${CAF_LIBRARIES})
ENDFOREACH()
