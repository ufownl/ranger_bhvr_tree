language: cpp

compiler:
    - gcc
    - clang

addons:
    apt:
        sources: ubuntu-toolchain-r-test
        packages: g++-4.8

before_script: if [ $CXX == "g++" ]; then export CXX="g++-4.8"; fi

script: cmake . && make && make test
